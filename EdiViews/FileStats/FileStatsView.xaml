<UserControl x:Class="EdiViews.FileStats.FileStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <StackPanel Orientation="Vertical" >
      <TextBlock Text="{Binding FileName}" Margin="3" ToolTip="File Name"/>
      <TextBlock Text="{Binding FilePath}" Margin="3" ToolTip="File Path"/>

      <TextBlock Text="{Binding FileSize}" Margin="3" ToolTip="File Size"/>
      <TextBlock Text="{Binding LastModified}" Margin="3" ToolTip="Date and time of last modification"/>
    </StackPanel>
  </Grid>
</UserControl>

